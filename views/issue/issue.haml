.form-element.title
    %label Title
    %input{:id => 'title', :type => 'text', :name => "issue[title]", :value => "#{@issue.title}"}
.form-element.description
    %label Description
    %br
    %textarea{:id => 'description', :name => "issue[description]", :rows => 10, :cols => 44}
        =@issue.description
.form-element.status
    %label Status
    %select{:id => 'status', :name => "issue[status]"}
        - %w(Open Closed).each do |status|
            - if @issue.status == status
                %option{:value => "#{status}", :selected => 'yes'}
                    =status
            - else
                %option{:value => "#{status}"}
                    =status

.form-element.save.save-issue
    %input{:type => 'submit', :value => 'Save'}
